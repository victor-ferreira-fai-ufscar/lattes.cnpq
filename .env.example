# Interpolacao do docker-compose.yml (nao e env_file de container)
# Copie para .env na raiz para customizar portas sem editar compose.

# Porta publicada e porta interna do backend
BACKEND_PORT=8000

# Porta publicada e porta interna do frontend
FRONTEND_PORT=3000
